specVersion: "v1beta"
serviceId: "cargo-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cargo-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
